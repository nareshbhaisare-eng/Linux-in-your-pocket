#### install htop, a real-time system process and resource monitor ####
sudo apt install htop
sudo yum install htop
sudo dnf install htop

####displays system performance statistics ####
vmstat 2 5

#### install and run iotop to monitor real-time disk I/O usage by processes ####
sudo apt install iotop
sudo yum install iotop
sudo dnf install iotop
sudo iotop

#### Viewing log sizes ####
sudo du -sh /var/log/*

#### clean and manage systemd journal logs by age, size, or complete removal and recreation ####
sudo journalctl --vacuum-time=7d
sudo journalctl --vacuum-size=200M
sudo rm -rf /var/log/journal
sudo mkdir /var/log/journal

#### Manual deletion of archived logs ####
sudo rm -f /var/log/*.gz
sudo rm -f /var/log/*.1

#### Auto-removing unused packages ####
sudo apt autoremove
sudo apt autoclean
sudo apt clean

#### remove unused packages and clean cached files ####
sudo yum autoremove
sudo yum clean all

#### remove unneeded packages and clean cached package files ####
sudo dnf autoremove
sudo dnf clean packages

#### ####
sudo apt install deborphan
deborphan
deborphan | xargs sudo apt -y remove –purge

#### identify and remove orphaned (unused) packages ####
sudo apt clean
rm -rf ~/.cache/thumbnails/*
sudo apt --purge autoremove

#### install and run BleachBit to clean unnecessary files and free up disk space ####
sudo apt install bleachbi
sudo bleachbit

#### Check current swap size ####
free -h
swapon --show

#### check and set the system swappiness value, controlling how aggressively Linux uses swap memory ####
cat /proc/sys/vm/swappiness
sudo sysctl vm.swappiness=10
sudo sysctl -p

#### Creating or expanding swap space ####
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

#### remove swap file ####
sudo swapoff /swapfile
sudo rm /swapfile

#### install sysstat and display extended CPU and I/O statistics ####
sudo apt install sysstat
iostat -x 2 5

#### Check disk space usage and Find space-consuming 
directories ####
df -h
du -sh /*

#### install and run iftop and nload to monitor real-time network traffic ####
sudo apt install iftop
sudo iftop
sudo apt install nload
nload

#### display network interface statistics and active listening/network connections with process info####
netstat -i
netstat -plant

#### Check latency and DNS ####
ping 8.8.8.8
ping google.com

#### search system logs for lines containing “error” or “fail” ####
grep -i error /var/log/syslog
grep -i fail /var/log/messages

#### view detailed system logs and logs for a specific service ####
journalctl -xe
journalctl -u nginx

#### continuously display new log entries in real time. ####
tail -f /var/log/syslog
journalctl -f

#### view all kernel parameters and temporarily modify system limits like max open files and swappiness####
sysctl -a
sudo sysctl -w fs.file-max=2097152
sudo sysctl -w vm.swappiness=10

#### Network tuning for high-performance servers ####
sudo sysctl -w net.core.somaxconn=1024
sudo sysctl -w
net.ipv4.tcp_max_syn_backlog=2048
sudo sysctl -p

#### Changes the priority of a running process identified by its PID ####
renice -n 10 -p <PID>

#### Limit memory usage ####
sudo cgcreate -g memory:/limited
echo 500M >
/sys/fs/cgroup/memory/limited/<bytes>
cgexec -g memory:limited ./script.sh

#### list all enabled services and disable a specific service####
systemctl list-unit-files --state=enabled
sudo systemctl disable bluetooth
 